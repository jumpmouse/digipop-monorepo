{
  "name": "server",
  "version": "0.0.1",
  "license": "MIT",
  "scripts": {
    "build:production": "rimraf ./api && tsc && yes | cp -rfa ./api/ ../../dist",
    "build:watch": "tsc --watch",
    "serve": "npm run build:watch & nodemon"
  },
  "dependencies": {
    "body-parser": "1.19.0",
    "express": "4.17.1"
  },
  "devDependencies": {
    "@types/babel-core": "^6.25.6",
    "@types/babel-template": "^6.25.2",
    "@types/express": "^4.17.3",
    "@types/jest": "^25.1.4",
    "@types/node": "^12.7.2",
    "nodemon": "^1.19.1",
    "rimraf": "^3.0.0",
    "ts-node": "^8.3.0",
    "typescript": "^3.6.2"
  }
}
